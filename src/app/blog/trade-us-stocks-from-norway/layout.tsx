import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Trade US Stocks from Norway | NOK to USDC Guide 2025',
  description: 'Norwegian guide to trading US stocks 24/7. Convert NOK to USDC with Vipps, start with 550 kr. Access NVIDIA, Tesla, Apple from Norway.',
  keywords: ['trade US stocks Norway', 'NOK to USDC', 'Vipps crypto', 'Norwegian stock trading', 'Hyperliquid Norway'],
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
