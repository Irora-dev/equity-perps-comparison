import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Trade US Stocks from Poland | PLN to USDC Guide 2025',
  description: 'Polish guide to trading US stocks 24/7. Convert PLN to USDC with BLIK, start with 200 zł. Access NVIDIA, Tesla, Apple from Poland.',
  keywords: ['trade US stocks Poland', 'PLN to USDC', 'BLIK crypto', 'Polish stock trading', 'Hyperliquid Poland'],
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
