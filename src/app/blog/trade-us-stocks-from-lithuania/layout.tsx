import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Trade US Stocks from Lithuania | EUR to USDC Guide 2025',
  description: 'Lithuanian guide to trading US stocks 24/7. Convert EUR to USDC with SEPA, start with €45. Access NVIDIA, Tesla, Apple from Lithuania.',
  keywords: ['trade US stocks Lithuania', 'EUR to USDC', 'Lithuanian crypto trading', 'Hyperliquid Lithuania'],
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
