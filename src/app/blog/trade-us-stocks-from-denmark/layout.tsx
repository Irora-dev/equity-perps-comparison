import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Trade US Stocks from Denmark | DKK to USDC Guide 2025',
  description: 'Danish guide to trading US stocks 24/7. Convert DKK to USDC with MobilePay, start with 350 kr. Access NVIDIA, Tesla, Apple from Denmark.',
  keywords: ['trade US stocks Denmark', 'DKK to USDC', 'MobilePay crypto', 'Danish stock trading', 'Hyperliquid Denmark'],
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
