import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Trade US Stocks from Sweden | SEK to USDC Guide 2025',
  description: 'Swedish guide to trading US stocks 24/7. Convert SEK to USDC with Swish, start with 500 kr. Access NVIDIA, Tesla, Apple from Sweden.',
  keywords: ['trade US stocks Sweden', 'SEK to USDC', 'Swish crypto', 'Swedish stock trading', 'Hyperliquid Sweden'],
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
