import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Trade US Stocks from Latvia | EUR to USDC Guide 2025',
  description: 'Latvian guide to trading US stocks 24/7. Convert EUR to USDC with SEPA, start with €45. Access NVIDIA, Tesla, Apple from Latvia.',
  keywords: ['trade US stocks Latvia', 'EUR to USDC', 'Latvian crypto trading', 'Hyperliquid Latvia'],
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
