import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Trade US Stocks from Europe | EUR to USDC Guide 2025',
  description: 'European guide to trading US stocks 24/7. Convert EUR to USDC with SEPA, start with €45. Access NVIDIA, Tesla, Apple from the EU.',
  keywords: ['trade US stocks Europe', 'EUR to USDC', 'SEPA crypto', 'European stock trading', 'Hyperliquid EU'],
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
