import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Trade US Stocks from Greece | EUR to USDC Guide 2025',
  description: 'Greek guide to trading US stocks 24/7. Convert EUR to USDC with SEPA, start with €45. Access NVIDIA, Tesla, Apple from Greece.',
  keywords: ['trade US stocks Greece', 'EUR to USDC', 'Greek crypto trading', 'Hyperliquid Greece'],
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
